screencopy {
    # Require an explicit prompt for each screen-share/screenshot request
    # instead of auto-granting it, so an app can't silently capture the
    # screen just by asking.
    allow_token_by_default = false
    custom_picker_binary = hyprland-preview-share-picker
}
